version: '3.3'

services:
  api:
    image: timoa/chrome-tab-rotate-server:latest@sha256:fad0d81945097d09285d1292e67748d6b0a82c38cafdd901d9bc921818d195ac
    environment:
      - NODE_ENV=production
      - NODE_HOST=0.0.0.0
    volumes:
      - ./config:/opt/app/config
    ports:
      - 9000:9000
    restart: always

volumes:
  config:
